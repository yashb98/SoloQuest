---
description: Implement a feature following SoloQuest engineering standards
---

Before writing any code:
1. Read CLAUDE.md for project conventions
2. Scan related files in the codebase to understand existing patterns
3. Create a plan with exact file paths and changes needed
4. Implement with proper TypeScript types and Tailwind styling
5. Ensure API routes use `export const dynamic = "force-dynamic"`
6. Verify dark mode compatibility (use `--sq-*` CSS variables)
7. Add toast notifications for user-facing actions
8. Run `npm run build` to verify no TypeScript errors

Feature to implement: $ARGUMENTS
